<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:c="http://www.springframework.org/schema/c"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/cache
       http://www.springframework.org/schema/cache/spring-cache.xsd">

    <!-- redis pool configuration -->
    <bean id="redisDataPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxWaitMillis" value="${redis.data.pool.max_wait_millis}"/>
        <property name="minIdle" value="${redis.data.pool.min_idle}"/>
        <property name="maxIdle" value="${redis.data.pool.max_idle}"/>
        <property name="maxTotal" value="${redis.data.pool.max_total}"/>
        <property name="testOnBorrow" value="${redis.data.pool.test_on_borrow}"/>
    </bean>

    <bean id="redisDataConnectionFactory"
          class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
        <property name="hostName" value="${redis.data.host}"/>
        <property name="port" value="${redis.data.port}"/>
        <property name="password" value="${redis.data.password}"/>
        <property name="timeout" value="${redis.data.timeout}"/>
        <property name="poolConfig" ref="redisDataPoolConfig"/>
    </bean>

</beans>